<%- include("templates/header") %>
    <section class="jumbotron text-center mt-3">
        <div class="container">
            <h1 class="jumbotron-heading">EXPLORE</h1>
            <p class="lead text-muted">Some text here maybe.</p>
            <p>
                <a href="#" class="btn btn-primary my-2">Item</a>
                <a href="#" class="btn btn-secondary my-2">Request</a>
            </p>
        </div>
    </section>
    <div class="album py-5 bg-light">
        <div class="container">
            <div class="row">
                
                <% items.forEach(function(item){ %>
                    <%- include("templates/homepageCard", {item: item}) %>
                  <% }) %>
                
                

            </div>
        </div>
</main>
<%- include("templates/footer") %>